import GMR from 'graphql-merge-resolvers';


const resolversTypes = GMR.merge([
    // resolverTypesDetalleVenta,
    // resolverTypesDetalleVentaAll,
    // resolverTypesBitacora,
    // resolverTypesMerma
]);

export default resolversTypes;